version: '3.8'

services:
  resume-parser:
    build: .
    container_name: resume-parser
    ports:
      - "8000:8000"
    environment:
      - LLAMA_CLOUD_API_KEY=${LLAMA_CLOUD_API_KEY}
    restart: always
